sudo: false
language: python
python:
  - 3.3
  - 3.4
  - 3.5

install:
  - pip install coveralls
  - git worktree add ci-test

script:
  - cd ci-test
  - sh ./scripts/test.sh

after_success:
  - coveralls
