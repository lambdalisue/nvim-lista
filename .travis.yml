sudo: false
language: python
python:
  - 3.3
  - 3.4
  - 3.5

install:
  - pip install coveralls
  - git clone --single-branch -b ci-test https://github.com/lambdalisue/neovim-prompt ci-test

script:
  - cd ci-test
  - sh ./scripts/test.sh

after_success:
  - coveralls
